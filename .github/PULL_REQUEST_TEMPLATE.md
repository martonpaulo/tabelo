<!-- Title: a Conventional Commits subject in English, ending with the issue numbers this pull
     request closes, e.g. `fix: normalize carriage returns (#54, #61)`. -->

Closes #

<!-- One `Closes #<n>` line per issue resolved. The title names the same numbers. -->

## What changed

<!-- The problem, and what this does about it. -->

## How it was verified

<!-- Replace this with what you ran and what it reported. -->

- [ ] `pnpm check` and `pnpm check-types` pass
- [ ] `pnpm check:dead-code` passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e` passes when user-visible behavior changed
- [ ] `pnpm build` passes
